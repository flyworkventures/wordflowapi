BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[User] ADD [wordPracticeProgress] FLOAT(53) NOT NULL CONSTRAINT [User_wordPracticeProgress_df] DEFAULT 0.0;

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
